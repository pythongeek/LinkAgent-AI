{
  "version": 2,
  "name": "linkedin-content-platform",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ],
  "buildCommand": "vite build && npx prisma generate --schema=./backend/prisma/schema.prisma && npx prisma migrate deploy --schema=./backend/prisma/schema.prisma",
  "installCommand": "npm install",
  "env": {
    "NODE_ENV": "production"
  }
}
